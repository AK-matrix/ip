# Enforce LF line endings for shell scripts
*.sh text eol=lf
# Enforce CRLF for batch files (optional but good practice on Windows)
*.bat text eol=crlf
# Enforce LF for text-based test files
*.txt text eol=lf
